# Client (copy to client/.env — never commit .env)
# Vite only exposes variables prefixed with VITE_ to the browser.
# Do not put MongoDB URIs, API secrets, or email credentials here.

VITE_SITE_URL=http://localhost:5173
VITE_API_URL=/api
VITE_MAP_EMBED_URL=
VITE_WEBINAR_URL=
VITE_WORKSHOP_URL=
