# Server (copy to server/.env — never commit .env)

NODE_ENV=development
PORT=5000
CLIENT_URL=http://localhost:5173,http://127.0.0.1:5173
MONGODB_URI=mongodb://127.0.0.1:27017/edlumina

TRUST_PROXY=false